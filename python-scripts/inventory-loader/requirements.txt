# Pinned: ipsdk>=0.5.0 uses `str | None` syntax in its own internals without
# `from __future__ import annotations`, which breaks under Python 3.9 (IAG's
# python-script venvs are built with the host's default `python3`, 3.9 here).
ipsdk==0.4.0
